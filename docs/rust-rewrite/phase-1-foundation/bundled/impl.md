# bundled: 实现方案（impl.md）

> **phase 归属（依赖序修正）**：本包**前移到 P1**（原列 P9）。原因：近叶子（`repo`/`tspath`/`vfs`），被 `ls/lsconv`(P7)/`lsp`/`api`(P8)/`cmd/tsgo`(P9) 依赖。详见根 README「依赖序口径」与 [references/crate-map.md](../../references/crate-map.md)。

**crate**：`tsgo_bundled`　**目标**：把 TypeScript 自带的 `lib.*.d.ts` 标准库文件**内嵌进二进制**，并通过一个虚拟文件系统（`bundled:///` 方案）暴露给编译器/语言服务，使发布的单文件可执行程序无需额外携带 stdlib。
**依赖（crate）**：`tsgo_vfs`（`vfs::FS` trait）、`tsgo_tspath`（路径规范化）。测试还用到 `tsgo_vfs::osvfs`。
**Go 源**：`internal/bundled/`（6 个非测试文件 + `libs/` 子目录 110 个 `.d.ts` 数据文件）。

## 这个包是什么（业务说明）

TypeScript 编译器在类型检查任何程序前，都要先加载一组标准库声明文件（`lib.es5.d.ts`、`lib.dom.d.ts`、`lib.esnext.d.ts` 等，共 110 个，最大的 `lib.dom.d.ts` 约 2.3MB）。Strada（TS 官方实现）把这些文件随 `tsc` 一起分发；typescript-go 把它们用 Go 的 `//go:embed` **编译进二进制**，运行时通过一个伪 FS 暴露在 `bundled:///libs/` 路径下，从而做到"零外部依赖的单文件 tsc"。

本包提供三件事：
1. **内嵌数据**：`embed_generated.go` 把每个 lib 文件 `//go:embed` 成一个 `string` 常量，汇总成 `embeddedContents map[string]string`。
2. **伪文件系统包装器**：`WrapFS(fs)` 返回一个 `vfs.FS`，凡是以 `bundled:///` 开头的路径都路由到内嵌内容，其余转发给真实 FS。`LibPath()` 返回 `bundled:///libs`。
3. **代码生成器**：`generate.go`（`//go:build ignore`，由 `go generate` 调用）从 TS 子模块的 `src/lib/libs.json` 读取 lib 清单，拼接版权头后写出 `libs/*.d.ts`，并生成 `libs_generated.go`（`LibNames`）与 `embed_generated.go`（embed 指令 + map）。

它处于 **Phase 9**，因为 cmd/tsgo（入口）、execute（CLI）、lsp、api 都通过 `bundled.WrapFS` + `bundled.LibPath` 装配文件系统；它本身只依赖 `vfs`/`tspath`，是 phase 内最叶子的包。

另有一个编译期开关：`embedded` 常量（`Embedded`）。`!noembed`（默认）走内嵌；`noembed`（构建标签）则改为从可执行文件所在目录读取磁盘上的 lib 文件——用于不希望把 2MB+ 数据塞进二进制的开发构建。

## 所有权 / 类型映射（本包关键决策）

通用规则见 PORTING.md §3。本包特有决策：

| Go 构造 | Rust 表示 | 说明 |
|---|---|---|
| `//go:embed libs/lib.X.d.ts` + `var libs_lib_X_d_ts string` | `include_str!("libs/lib.X.d.ts")` | 编译期把文本嵌入只读数据段，等价 go:embed string。文件大但都是文本（UTF-8）。 |
| `embeddedContents map[string]string` | `static EMBEDDED_CONTENTS: phf::Map<&str, &str>` 或 `LazyLock<FxHashMap<&'static str, &'static str>>` | key=`"libs/lib.X.d.ts"`，value=`include_str!`。用 `phf`（编译期完美哈希）零运行时构建成本；退而求其次 `LazyLock<FxHashMap>`。**只读、键固定**，不需要 IndexMap。 |
| `LibNames []string`（按名排序） | `pub static LIB_NAMES: &[&str]`（生成、按名排序） | 顺序必须与 `WalkDir` 遍历顺序一致（见 `TestEmbeddedLibs`）。 |
| `libsEntries []fs.DirEntry` | `static LIBS_ENTRIES: &[FileInfo]`（生成） | 用于 `walk_dir`，每项含 name + size。 |
| `const embedded = true/false`（构建标签 `noembed`） | `#[cfg(feature = "embed")]` / `#[cfg(not(feature = "embed"))]` + `pub const EMBEDDED: bool` | Go build tag → Cargo feature `embed`（默认开）。两套 `wrap_fs`/`lib_path`/`is_bundled` 实现按 feature 分发。 |
| `wrappedFS` 实现 `vfs.FS` 18 个方法 | `struct WrappedFs<F: FS> { inner: F }` impl `FS` | 直接实现 `vfs::FS` trait（**不**绕道 `std::io::fs`，与 Go 注释一致——`vfs::FS` 以 `String` 为内容单位，避免 `[]byte` 来回拷贝）。 |
| `splitPath` (`strings.CutPrefix`) | `path.strip_prefix("bundled:///")` → `Option<&str>` | |
| `sync.OnceValue` (`bundledSourceDir`/`testingLibPath`/`libPath`) | `LazyLock<String>` 或 `OnceLock<String>` | |
| `runtime.Caller(0)` 取源文件目录（仅测试用） | `env!("CARGO_MANIFEST_DIR")` 拼 `libs` | Rust 在编译期就知道 crate 根目录，比 `runtime.Caller` 更稳。 |
| `os.Executable()` + `Realpath`（noembed） | `std::env::current_exe()` + `realpath` | |
| `fileInfo`（实现 `fs.FileInfo`+`fs.DirEntry`） | `struct FileInfo` impl `vfs::FileInfo` + `vfs::DirEntry` | mode/name/size 三字段。 |
| `fs.SkipAll` / `fs.SkipDir`（WalkDir 控制流） | `vfs::WalkDir` 的控制枚举（P1 vfs 决定） | `walk_dir` 的 early-stop 语义需与 vfs 抽象一致。 |
| panic on write to embedded FS | `panic!("cannot write to embedded file system")` | `write_file`/`append_file`/`remove`/`chtimes` 对 `bundled:///` 路径 panic，与 Go 一致。 |

### 内嵌方案（重点，cmd/tsgo 发布形态的命门）

Go 用三条流水线：`go:embed`（数据进二进制）+ 构建标签（embed/noembed 切换）+ `go generate`（从 TS 子模块再生成）。Rust 等价方案：

**A. 数据内嵌（对应 `embed_generated.go`，默认 `feature = "embed"`）**
- 生成一个**已提交**的 `internal/bundled/embed_generated.rs`，内容形如：
  ```rust
  // Code generated by generate; DO NOT EDIT.
  pub(crate) static EMBEDDED_CONTENTS: phf::Map<&'static str, &'static str> = phf::phf_map! {
      "libs/lib.d.ts" => include_str!("libs/lib.d.ts"),
      "libs/lib.es5.d.ts" => include_str!("libs/lib.es5.d.ts"),
      // ... 110 条，与 LibNames 同序
  };
  pub(crate) static LIBS_ENTRIES: &[FileInfo] = &[
      FileInfo::file("lib.d.ts", include_str!("libs/lib.d.ts").len() as i64),
      // ...
  ];
  ```
  `include_str!` 在 `rustc` 链接期把文本并入 `.rodata`，与 `go:embed` 完全同语义（编译进二进制、运行时零 I/O、`&'static str` 借用）。
- 备选：`include_bytes!`（若想避免 UTF-8 校验开销）→ 需在读取处 `str::from_utf8`。lib 文件保证 UTF-8，故首选 `include_str!`。
- `rust-embed` crate 也能做，但它默认走 `fs::FS` 风格、运行时构建索引，且 debug 模式回退磁盘——与本包"直接实现 vfs::FS、避免拷贝"的诉求不符，**不采用**。

**B. 编译期开关（对应 `//go:build noembed` / `embed.go` vs `noembed.go`）**
- Cargo feature `embed`（默认在 `[features] default = ["embed"]`）。
- `#[cfg(feature = "embed")] mod embed;`（含 `EMBEDDED = true`、`bundled:///` 方案、`WrappedFs`）。
- `#[cfg(not(feature = "embed"))] mod noembed;`（含 `EMBEDDED = false`、`wrap_fs` 直接返回原 FS、`lib_path()` 用可执行文件目录、`is_bundled` 恒 `false`）。
- `lib.rs` 重导出统一 API：`wrap_fs` / `lib_path` / `is_bundled` / `EMBEDDED`。

**C. 再生成器（对应 `generate.go` + `//go:generate`）**
- 一个 **xtask 风格的二进制** `internal/bundled/src/bin/generate.rs`（或 workspace 级 `xtask generate-libs`），手动运行（等价 `go generate`），它：
  1. 读 `repo::typescript_submodule_path()/src/lib/libs.json`（JSONC，需先 strip 注释 + 尾逗号——见 `stripJSONC`）。
  2. 读 `scripts/CopyrightNotice.txt`，CRLF→LF。
  3. 对每个 lib：拼接版权头 + `\n` + 源 `.d.ts`（CRLF→LF），写 `libs/<target>`。
  4. 按 `target` 名排序后，生成 `libs_generated.rs`（`LIB_NAMES`）与 `embed_generated.rs`（`include_str!` 映射 + `LIBS_ENTRIES`）。
- **不**用 `build.rs` 自动跑：与 Go 的 `go:generate`（手动、产物入库）保持一致，避免每次构建都去碰 TS 子模块。`build.rs` 仅作为备选在 impl 里注明。

> 偏离记录：`bundled:///` 是自定义 URL scheme，不是真实路径；Rust 侧 `WrappedFs` 必须对所有 `vfs::FS` 方法做 scheme 前缀判定后路由，结构与 Go `wrappedFS` 1:1。

## 文件清单 → Rust 模块

| Go 文件 | Rust 文件 | 说明 |
|---|---|---|
| `internal/bundled/bundled.go` | `internal/bundled/lib.rs`（crate 根，basename==目录名→`lib.rs`） | 公共 API：`WrapFS`/`LibPath`/`Embedded`/`TestingLibPath`；`mod` 声明 + 重导出。 |
| `internal/bundled/embed.go`（`!noembed`） | `internal/bundled/embed.rs`（`#[cfg(feature="embed")]`） | `bundled:///` 方案、`WrappedFs`、`FileInfo`、`walk_dir`、`split_path`、`is_bundled`。 |
| `internal/bundled/noembed.go`（`noembed`） | `internal/bundled/noembed.rs`（`#[cfg(not(feature="embed"))]`） | 磁盘回退：`wrap_fs` 透传、`lib_path()` 取可执行目录、`is_bundled`恒false。 |
| `internal/bundled/embed_generated.go`（生成） | `internal/bundled/embed_generated.rs`（生成、入库） | `EMBEDDED_CONTENTS` + `LIBS_ENTRIES`（`include_str!`）。 |
| `internal/bundled/libs_generated.go`（生成） | `internal/bundled/libs_generated.rs`（生成、入库） | `LIB_NAMES: &[&str]`（按名排序）。 |
| `internal/bundled/generate.go`（`//go:build ignore`） | `internal/bundled/src/bin/generate.rs`（xtask） | 再生成器；手动运行，产物入库。 |
| `internal/bundled/libs/*.d.ts`（110 数据文件） | 原样保留（被 `include_str!` 引用） | 不翻译，仅作内嵌数据。 |

## 依赖白名单（本包新增的 crate）

| 用途 | crate | 备注 |
|---|---|---|
| 编译期完美哈希（内嵌 map） | `phf` + `phf_macros` | `EMBEDDED_CONTENTS`，零运行时构建；备选 `std::sync::LazyLock<FxHashMap>`。 |
| JSONC 解析（generate） | 复用 `tsgo_json` 或 generate 内手写 `strip_jsonc` | 仅 generate 二进制用。 |

> 新增依赖执行期 `cargo add`，记录到 `references/crate-map.md`。

## 实现 TODO（逐文件 / 逐函数，可勾选）

> **实现状态（P1 收口，2026-05）**：已落地 **embed-only 变体**（默认内嵌），`cargo test -p tsgo_bundled`（2 单测 + 7 doctest）全绿、`cargo clippy -p tsgo_bundled --all-targets -- -D warnings` 干净。
> 按任务范围**未做** `noembed` feature 分支与 `src/bin/generate.rs` xtask（见各小节说明与「与 Go 的已知偏离」）。生成的 `embed_generated.rs`/`libs_generated.rs` 由一次性脚本产出并入库。

### `lib.rs`（Go: `internal/bundled/bundled.go`）

- [x] `pub const EMBEDDED: bool`（恒 `true`，embed-only）　`// Go: bundled.go:Embedded`
- [x] `pub fn wrap_fs<F: Fs>(fs: F) -> WrappedFs<F>`（重导出自 `embed`；返回具名类型而非 `impl Fs`）　`// Go: bundled.go:WrapFS`
- [x] `pub fn lib_path() -> String`（= `bundled:///libs`，重导出自 `embed`）　`// Go: bundled.go:LibPath`
- [x] `pub fn testing_lib_path() -> String`（`normalize_slashes(concat!(CARGO_MANIFEST_DIR,"/libs"))`）　`// Go: bundled.go:TestingLibPath`
- [~] `static BUNDLED_SOURCE_DIR` — **偏离**：直接内联 `concat!(env!("CARGO_MANIFEST_DIR"),"/libs")`，省去 OnceValue。
- [x] `mod embed;` + 重导出 `wrap_fs`/`lib_path`/`is_bundled`/`WrappedFs`/`LIB_NAMES`（**无** `noembed` 分发，embed-only）。

### `embed.rs`（Go: `internal/bundled/embed.go`，embed-only，无 cfg gate）

- [x] `const SCHEME: &str = "bundled:///"`　`// Go: embed.go:scheme`
- [x] `fn split_path(path: &str) -> Option<&str>`（`strip_prefix`）　`// Go: embed.go:splitPath`
- [x] `pub fn is_bundled(path: &str) -> bool`　`// Go: embed.go:IsBundled`
- [x] `pub fn lib_path() -> String`（= `"bundled:///libs"`）　`// Go: embed.go:libPath`
- [x] `struct WrappedFs<F: Fs> { inner: F }` + `impl Fs for WrappedFs`：
  - [x] `use_case_sensitive_file_names` — 透传
  - [x] `file_exists` — 内嵌路径查 `EMBEDDED_CONTENTS`
  - [x] `read_file` — 内嵌路径返回内嵌内容（`Option<String>`）
  - [x] `directory_exists` — 内嵌路径仅 `"libs"` 为真
  - [x] `get_accessible_entries` — `""`→dirs=["libs"]，`"libs"`→files=LIB_NAMES
  - [x] `stat` — 内嵌路径返回 `vfs::FileInfo`(dir 或 file+size)
  - [x] `walk_dir` — 遍历内嵌树，处理 SkipAll/SkipDir（`WalkControl`）
  - [x] `realpath` — 内嵌路径原样返回
  - [x] `write_file`/`append_file`/`remove`/`chtimes` — 内嵌路径 `panic!`，否则透传
- [x] `fn walk_embedded(rest, walk_fn)` 递归内部实现　`// Go: embed.go:walkDir`
- [~] `FileInfo` — **偏离**：直接复用 `tsgo_vfs::FileInfo`（vfs 已是具名 struct），不再自写。
- [x] `static ROOT_ENTRIES`（`LazyLock<Vec<FileInfo>>`，仅 `libs` 目录项）　`// Go: embed.go:rootEntries`
- [x] `static LIBS_ENTRIES`（`LazyLock<Vec<FileInfo>>`，由 `EMBEDDED_FILES` 派生 name+size）　`// Go: embed.go:libsEntries`

### `noembed.rs`（Go: `internal/bundled/noembed.go`）

- [—] **未实现（按任务范围跳过）**：本轮只做 embed-only 变体；`noembed` 磁盘回退留待后续需要时再补。

### `embed_generated.rs` / `libs_generated.rs`（生成、入库）

- [x] `EMBEDDED_CONTENTS`（`LazyLock<FxHashMap<&str,&str>>`，由 `EMBEDDED_FILES` 的 108 `include_str!` 派生；键 `"libs/<name>"`）
- [x] `EMBEDDED_FILES`（108 `("libs/<name>", include_str!(...))`，按名序）
- [x] `pub static LIB_NAMES: &[&str]`（108，按名序）　`// Go: libs_generated.go`

### `src/bin/generate.rs`（xtask，Go: `internal/bundled/generate.go`）

- [—] **未实现（按任务范围跳过）**：`libs/` 与两份生成文件已就绪；本轮用一次性脚本从 `libs_generated.go` 的 `LibNames` 派生入库，未移植 JSONC 再生成器。

### Cargo / crate 接线

- [x] `internal/bundled/Cargo.toml`：`name = "tsgo_bundled"`；deps `tsgo_repo`/`tsgo_tspath`/`tsgo_vfs` + `cargo add rustc_hash`（未用 `phf`）。
- [x] 根 `Cargo.toml` workspace members 已含 `internal/bundled`（无需改动）。
- [x] `lib.rs` 重导出统一 API（embed-only，无 cfg 分发）。

## TDD 推进顺序（tracer bullet → 增量）

1. 先落 `libs_generated.rs`（`LIB_NAMES`）+ `lib.rs` 的 `testing_lib_path()`，过 `TestTestingLibPath`（只验证 `<libs>/lib.d.ts` 存在）——不需要内嵌，最快闭环。
2. 落 `embed_generated.rs`（先放几个 lib 占位即可编译）+ `WrappedFs::walk_dir` + `is_dir`，过 `TestEmbeddedLibs`（`WalkDir(LibPath())` 收集到的文件名 == `LIB_NAMES`）。
3. 补全 `WrappedFs` 其余方法（read_file/file_exists/stat/...）+ panic 路径。
4. 跑 `generate-libs` xtask 再生成全量 `embed_generated.rs`，确认 `include_str!` 全 110 文件编译通过。
5. 加 `noembed` feature 分支编译验证（`cargo build --no-default-features`）。

## 与 Go 的已知偏离（divergence — 实测落地版）

- **只做 embed-only 变体**：本轮按任务范围**不**实现 `noembed.go`/`generate.go` 的 Rust 对应物，也**不**引入 Cargo feature。`EMBEDDED` 恒 `true`。后续若需磁盘回退/再生成器再补。
- **`runtime.Caller(0)` → `CARGO_MANIFEST_DIR`**：`testing_lib_path()` 直接 `normalize_slashes(concat!(env!("CARGO_MANIFEST_DIR"),"/libs"))`，省去 `bundledSourceDir` OnceValue；Go 的"非测试 panic"守卫被丢弃（任务允许）。
- **内嵌 map 用 `LazyLock<FxHashMap>`（`rustc_hash`）而非 `phf`**：键集合固定、值为 `include_str!` 的 `&'static str`，行为与 Go `embeddedContents` 一致；`include_str!` 等价 `//go:embed`（编译进 `.rodata`、运行时零 I/O）。
- **`fileInfo` → 复用 `tsgo_vfs::FileInfo`**：vfs 已把 Go 的 `fs.FileInfo`/`fs.DirEntry` 收敛为一个具名 struct，故不再自写；`libsEntries`/`rootEntries` 改为 `LazyLock<Vec<FileInfo>>`。
- **`WalkDir` 控制流**：`tsgo_vfs` 的回调签名是 `FnMut(&str, &FileInfo) -> FsResult<WalkControl>`（`Continue`/`SkipDir`/`SkipAll` 枚举，而非 `fs.SkipAll`/`fs.SkipDir` 哨兵 error）。`walk_embedded` 用 `Ok(WalkControl::SkipAll)` 向上传播以整体中止，公开 `walk_dir` 把它折叠为 `Ok(())`，与 Go「`WalkDir` 捕获 `SkipAll` 返回 nil」语义一致。
- **`read_file` 返回 `Option<String>`**（vfs trait 口径），对应 Go 的 `(string, bool)`。
- **`wrap_fs` 返回具名 `WrappedFs<F>`** 而非 `impl Fs`，便于在 doctest/调用点命名类型。

## 转交 / 推迟（DEFER）

- `tsgo_vfs::FS` trait 与 `WalkDir`/`FileInfo`/`DirEntry`/`osvfs` 来自 **P1**；本包实现 `WrappedFs` 时 `// DEFER(phase-1) blocked-by: tsgo_vfs` 仅指依赖就绪即可，本包逻辑不推迟。
- `repo::typescript_submodule_path()`（generate 用）来自 **P1 `tsgo_repo`**。
- 实际 110 个 `.d.ts` 内容文件不翻译，作为内嵌数据资产长期保留。
